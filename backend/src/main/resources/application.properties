spring.application.name=backend
server.port=${PORT:8080}

# PostgreSQL Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-plain-lab-a43ecmoa-pooler.us-east-1.aws.neon.tech/quiz-app-db?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_zZsxm7AcSPX9}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate ddl-auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false


# Ignore exceptions if db is not connected yet, allow app to start for testing
# (spring.datasource.continue-on-error=true)
